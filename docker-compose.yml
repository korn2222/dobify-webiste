services:
  dobify-web:
    build:
      context: .
      dockerfile: Dockerfile
    image: dobify-web:latest
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - NEXT_PUBLIC_SITE_URL=https://dobify.com
    restart: always
